#!/usr/bin/env sh

GITBASH="$PROGRAMFILES/Git/bin/bash.exe"

if echo "$PWD" | grep -q "System32$"
then
  cd "$HOME"
fi

if [ -f "$GITBASH" ]
then
  "$GITBASH" --login -i "$@"
else
  bash --login -i "$@"
fi
